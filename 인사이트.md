# 💡 Claude4Net 레퍼런스 프로젝트 딥다이브 인사이트

## 1. 레퍼런스 프로젝트 아키텍처 요약 (Core Structure & Philosophy)

### **OpenClaude (TypeScript/React 기반 CLI)**
* **목적형 서브 에이전트 분리:** 기획(Plan), 탐색(Explore), 검증(Verification) 등 역할별로 특화된 에이전트(Sub-agent)를 정의하고, 메인 루프에서 필요에 따라 동적으로 스폰(Spawn)하는 아키텍처입니다.
* **상태와 UI의 완전한 분리:** React 기반 CLI(Ink)를 사용하여 Agent의 논리적 진행 상태(ProgressTracker)와 CLI 렌더링을 완전히 분리해 두었습니다.
* **유연한 API 라우팅:** 단일 LLM에 의존하지 않고, 작업의 종류와 하위 에이전트의 성격에 따라 서로 다른 LLM API 설정(Provider Override)을 주입할 수 있습니다.

### **Claw-Code-Main (Python 기반 포팅 프로젝트)**
* **Query Engine (의도 라우팅):** 사용자의 프롬프트를 곧바로 거대 LLM에 넘기지 않고, 자체 Query Engine을 통해 내부 명령어(Command)인지, 도구 사용(Tool)인지 사전 평가(Scoring)하여 실행 경로를 분기합니다.
* **런타임 모드 추상화:** SSH, Remote, Direct Connect, Teleport 등 다양한 실행 환경(Runtime Mode)을 인터페이스로 추상화하여, 에이전트 루프가 환경에 종속되지 않고 실행되도록 설계되었습니다.

---

## 2. 핵심 구현 패턴 및 흥미로운 점

### **A. Tool 처리 및 동시성 (Concurrency) 최적화**
* `openclaude`의 `toolOrchestration.ts`를 보면 도구(Tool) 호출 시 `isConcurrencySafe` (동시 실행 안전성) 속성을 확인합니다.
* 파일 읽기, 검색, 상태 조회와 같이 상태를 변경하지 않는(Read-only) 도구들은 LLM이 한 번에 여러 개를 요청했을 때 병렬(Concurrent)로 동시 실행하여 응답 속도를 극대화합니다.

### **B. 컨텍스트 및 시스템 프롬프트 관리 (System Prompt Hierarchy)**
* `systemPrompt.ts`에서 시스템 프롬프트를 단일 문자열로 하드코딩하지 않고 계층형으로 조립합니다.
* **[Override Prompt] -> [Coordinator Prompt] -> [Sub-Agent Prompt] -> [Default Prompt]** 순으로 우선순위를 가지며, 특정 모드(Proactive Mode)에서는 기존 프롬프트를 덮어쓰지 않고 동적으로 지침을 "Append"하여 컨텍스트 일관성을 유지합니다.

### **C. 에이전트 루프 및 토큰 추적 (Agent Loop)**
* `LocalAgentTask.tsx`에서 API의 토큰 과금 방식을 고려하여 입력 토큰(Input Tokens)과 출력 토큰(Output Tokens)을 분리 추적합니다.
* 누적되는 Context로 인해 계속 증가하는 입력 토큰은 갱신(Latest) 처리하고, 턴마다 발생하는 출력 토큰은 합산(Cumulative)하여 정확한 비용/사용량을 계산합니다.
* 무한 루프를 방지하기 위해 가장 최근에 실행한 도구 활동 내역(`recentActivities`)을 롤링 윈도우 방식으로 5개만 유지합니다.

---

## 3. Claude4Net 도입 제안 (비동기 채널/플러그인 시스템 벤치마킹)

현재 우리의 `Claude4Net` (C# Event-Driven, Producer-Consumer 채널 패턴) 구조에 즉시 적용하여 성능과 안정성을 끌어올릴 수 있는 핵심 실전 아이디어들입니다.

**① 읽기 전용 플러그인의 병렬 실행 (Concurrent Tool Execution 파이프라인)**
* **개요:** LLM이 한 번의 턴에서 여러 개의 도구 사용을 요청했을 때, 순차적으로 실행하면 I/O 지연이 발생합니다.
* **적용 방안:** `Claude4Net.Tools`의 인터페이스(`IPluginTool`)에 `bool IsConcurrencySafe { get; }` 속성을 추가합니다. `FileReadTool`, `LsTool`, `GrepTool` 등은 `true`로 설정하고, `FileEditTool`, `BashTool` 등은 `false`로 설정합니다.
* **효과:** `ToolOrchestrator.cs`에서 `IsConcurrencySafe == true`인 도구 요청들을 묶어 C#의 `Task.WhenAll`로 병렬 처리하면 응답성이 획기적으로 향상됩니다.

**② 의도 기반 선행 라우터 (Pre-routing Query Engine)**
* **개요:** `claw-code-main`의 패턴을 차용하여 모든 입력을 무거운 메인 LLM에 넘기지 않습니다.
* **적용 방안:** 사용자의 입력을 `Claude4Net.Api` 계층에 도달하기 전, 경량 로컬 모델(예: `OllamaProvider`)이나 정규식 기반의 `QueryEngine`을 거치게 합니다. 단순 시스템 명령어(예: "현재 날시 알려줘", "빌드해줘")는 LLM 추론 없이 즉시 `CommandRegistry`로 직행시킵니다.

**③ 동적 모델 라우팅 체계 (Dynamic Model Routing)**
* **개요:** 단일 프로바이더(예: Gemini) 환경에서도 작업의 성격(탐색, 코드 작성, 검증 등)에 따라 적절한 수준의 LLM 모델을 유연하게 스위칭합니다.
* **적용 방안:** `openclaude`의 `agentRouting.ts` 패턴을 차용하여 C# `SettingsManager.cs`에 에이전트별 모델 라우팅 테이블을 구현합니다. 예컨대 코드베이스를 훑어보거나 단순 파일 분석을 하는 '탐색 작업'은 빠르고 저렴한 모델(예: `Gemini 2.5 Flash` 또는 `Gemini 3 Flash`)에 할당하고, 복잡한 로직을 설계하고 작성하는 '핵심 작업'은 고성능 모델(예: `Gemini 2.5 Pro` 또는 `Gemini 3 Pro`)로 라우팅하는 전략을 채택합니다.

**④ HTTP 소켓 고갈(Socket Exhaustion) 방지 아키텍처**
* **개요:** 현재 `Claude4Net.Api` 내부(`AtomicChatProvider.cs` 등)에서 `using var client = new HttpClient();` 형태로 HTTP 클라이언트를 매번 생성/폐기하거나, 인스턴스별로 생성하는 패턴이 발견되었습니다. 이는 높은 빈도의 LLM 및 도구 API 호출 환경에서 TCP `TIME_WAIT` 상태 포트 고갈을 유발합니다.
* **적용 방안:** C# .NET 환경에서 권장하는 `IHttpClientFactory` 패턴을 DI(Dependency Injection) 컨테이너에 도입합니다. 또는, Singleton `HttpClient`와 `SocketsHttpHandler(PooledConnectionLifetime 설정)`를 조합하여 연결 풀(Connection Pool)을 재사용하도록 아키텍처를 전면 수정해야 합니다.

**⑤ 컨텍스트 압축 및 축약 (Context Collapse)**
* **개요:** 에이전트가 탐색 과정에서 파일 목록 조회, 검색, 파일 읽기를 반복하면 토큰이 낭비되고 Context Window가 급격히 소진됩니다.
* **적용 방안:** `openclaude`의 `collapseReadSearch.ts` 패턴을 참고하여, `LsTool`, `GrepTool`, `FileReadTool` 등 탐색/읽기 전용 도구들의 연속적인 실행 기록은 LLM 컨텍스트에 다시 전달될 때 하나의 요약된 블록(Collapsed Group)으로 압축하거나, 이전 검색 결과의 원문을 제거하고 메타데이터(수행 여부)만 남기는 `ContextCompressor` 파이프라인을 `AgentLoop`에 추가합니다.

**⑥ AST 기반 쉘 명령어 권한 검증 및 작업 공간 제한적 YOLO(루트 권한) 모드**
* **개요:** LLM이 생성한 쉘 명령어를 터미널에 무비판적으로 실행(`BashTool` 등)하는 것은 치명적인 보안 리스크를 수반합니다.
* **적용 방안:** `openclaude`의 모델을 벤치마킹하여, `BashTool` 실행 전 명령어를 구문 트리(AST) 수준에서 파싱 및 분석하여 사용자 승인(Prompt)을 강제하는 `PermissionManager` 계층을 도입합니다.
* **작업 공간 제한적 YOLO (Bounded Auto Mode):** `--yolo` 플래그로 완전 자율 주행 모드를 켜더라도, 루트 권한(승인 우회)은 **현재 작업 디렉토리(Workspace) 내부로 엄격히 제한(Blast Radius Containment)**합니다. 만약 에이전트가 작업 디렉토리 외부의 파일을 수정하거나 파괴적인 명령(`rm -rf /` 등)을 시도할 경우, 즉시 루트 권한을 해제(강등)하고 읽기만 허용하거나 사용자 승인 플로우로 폴백(Fallback)하도록 설계합니다.

**⑦ 프롬프트 캐시 파괴 감지 및 최적화 (Prompt Cache Break Detection)와 TeruTeruPandas 결합**
* **개요:** LLM API(Gemini, Claude 등)의 컨텍스트 캐싱(Context Caching)을 활용할 때, 매 턴마다 시스템 프롬프트나 도구(Tool) 목록이 미세하게 바뀌면 캐시가 파괴(Cache Miss)되어 비용과 지연 시간이 급증합니다.
* **적용 방안:** `openclaude`의 `promptCacheBreakDetection.ts` 패턴을 차용하여, 턴이 넘어갈 때마다 시스템 프롬프트의 Hash값과 로드된 플러그인(Tool) 스키마의 Hash값을 비교하는 로직을 추가합니다. 
* **TeruTeruPandas 기반 캐시 분석(Telemetry):** 프로젝트 내에 이미 포함된 고성능 인메모리 데이터 처리 엔진인 **`TeruTeruPandas`를 적극 활용**합니다. 매 턴마다 발생한 캐시 히트율(Hit Rate), 토큰 사용량, 해시 변경 이력을 DataFrame 형태로 로깅(Caching)하고, 이를 바탕으로 "어떤 에이전트의 어떤 행동이 캐시를 자주 깨뜨리는지" 실시간 통계를 내거나 병목 구간을 분석(Telemetry)하는 용도로 연결하면 엄청난 시너지를 낼 수 있습니다.

---

## 4. 당장 실행 가능한 리팩토링 액션 아이템 (For Implementation Agent)

> **💡 설계자 노트:** 본 문서의 내용은 시스템 아키텍처와 리팩토링 방향을 정의한 설계 지침서입니다. 실제 코드 레벨의 구현 작업은 현재 기존 명령어 구현을 담당하고 있는 **"Gemini 3 Flash" (또는 3 Flash 기반 작업 에이전트)** 가 본 문서를 참조하여 순차적으로 수행할 예정입니다.

1. **`ToolOrchestrator.cs` 리팩토링:** 
   - 도구 실행 큐(Queue)에서 도구 목록을 꺼낼 때 `IsConcurrencySafe` 속성을 검사하여, 병렬 실행 가능한 태스크들을 `Task.WhenAll`로 묶어서 실행하도록 로직 수정.
2. **`Claude4Net.Tools` 인터페이스 보강:**
   - 기존의 모든 `*Tool.cs` 파일들 (`BashTool.cs`, `FileReadTool.cs` 등)에 `IsConcurrencySafe` 프로퍼티 구현 반영 및 Read-Only 여부 명시.
3. **`Claude4Net.Runtime.AgentLoop.cs` 내 토큰/상태 트래커 도입:**
   - LLM 턴(Turn)마다 소모되는 Input/Output 토큰을 분리하여 추적하는 `ProgressTracker` 클래스 신설.
   - 최근 사용한 도구 목록을 `CircularBuffer` 형태로 보관하여 Context 비대화를 방지하고, 디버깅 및 UI 진행 상태 바(Progress Bar) 표시에 활용.
4. **HTTP 소켓 고갈 방지 (DI 컨테이너 및 `IHttpClientFactory` 전면 도입):**
   - `Claude4Net.Api` 내의 `OllamaProvider.cs`, `GeminiProvider.cs`, `AtomicChatProvider.cs` 등에서 `new HttpClient()` 패턴을 제거.
   - `AddHttpClient()`를 사용해 DI 컨테이너에 HTTP 클라이언트를 등록하고, 프로바이더들이 생성자를 통해 주입받도록 리팩토링.
5. **프롬프트 합성 계층 구현 (`Claude4Net.SDK/AppState.cs` 등):**
   - 하드코딩된 시스템 프롬프트를 제거하고, `SystemPromptBuilder` 클래스를 신설하여 [글로벌 지침] + [현재 작업 컨텍스트] + [특화 에이전트 지침]을 동적으로 Append 하는 구조로 개편.
6. **컨텍스트 압축 및 권한 검증 미들웨어 도입 (`Claude4Net.Runtime`):**
   - `AgentLoop.cs` 내부 메시지 히스토리 처리 로직에 `ContextCompressor`를 추가하여 조회성 도구 로그를 요약/삭제.
   - `BashTool.cs` 및 `FileWriteTool.cs` 실행 전 검사에 경로 기반(Path-based) 검증을 추가하여, **YOLO 모드라도 작업 디렉토리 내부에서만 승인을 우회(Bypass)하고 외부 접근 시에는 승인 플로우로 폴백**하도록 조건 분기 구현.