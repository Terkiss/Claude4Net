# Claude4Net Implementation Plan

- Plan date: 2026-05-07
- Working branch: `experiment`
- Source insight: `Documents/인사이트_0506_final.md`
- Current baseline: `master` and `experiment` are aligned at `c1726e0`
- Windows release branch: `master`
- Linux release branch: `Linuxmaster`
- macOS release branch: `macoMaster`
- Current execution source of truth: this file

## 0. Product Direction

Claude4Net-App is moving from a Windows-stable agent CLI into an enterprise-grade .NET agent framework.

The product identity is:

> Claude4Net-App is a trusted execution environment where .NET developers can safely and transparently delegate real development work to AI agents.

The next roadmap is based on three product pillars from the 2026-05-06 insight report:

1. Intelligence: reusable and self-improving skill assets
2. Orchestration: observable, resumable, file-backed agent work
3. Safety: explicit permission, approval, diff, and doctor gates

GUI is out of scope for at least one year. The roadmap focuses on `Claude4Net.Cli`, `Spectre.Console` TUI, Discord integration, and machine-readable status.

## 1. Stable Baseline

The K012 Windows baseline is complete and remains the release foundation.

Completed foundation:

- D01 Baseline, Project Safety, Build/Test Standards
- D02 Memory Sharing and RAG Stability
- D03 Sandboxing and Permission State Machine
- D04 Diagnostics, Source Guard, Masking
- D05 SmartRouter
- D06 Resource-Oriented Skills
- D07 Discord Async Orchestration
- D08 Coordinate and Evidence Gates
- D09 Self-Healing and Trajectory Quality
- D10 Testing, Documentation, Release Gate
- K012 Final Stabilization and Handoff
- K013 maintenance fixes through Gemini tool-use stability

Branch policy:

- `experiment`: all new feature work starts here
- `master`: Windows stable release branch
- `Linuxmaster`: Linux release-preparation branch
- `macoMaster`: macOS release-preparation branch

Do not implement new features directly on `master`, `Linuxmaster`, or `macoMaster`.

## 2. Official Verification Standard

Windows official release gate:

```powershell
.\scripts\verify-release.ps1
```

That gate must validate:

1. Standard build
2. Strict nullable build
3. Unit and integration tests
4. CLI smoke test using `--smoke-exit`

Do not use these as official release criteria:

- `dotnet run` pipe validation
- `/exit` pipe validation
- direct stdin pipe validation

Unix preparation gate:

```bash
./scripts/verify-release.sh
```

Unix gate status:

- available on `macoMaster`
- native macOS verification pending
- Linux branch preparation still pending

## 3. Operating Roles

### Gemini CLI Worker

- Implements assigned work
- Modifies code, tests, and docs
- Runs the official release gate
- Does not commit or push

### Gemini 3.1 Pro First Reviewer

- Reviews worker output
- Checks staged and unstaged diffs
- Re-runs the release gate when needed
- Writes P1/P2/P3 findings
- If work is not ready, writes the correction prompt
- Does not escalate to Codex until blocking findings are cleared
- Does not commit or push

### Codex Final Controller

- Reads the actual repository state before trusting reports
- Makes final approval, commit, and push decisions
- May directly perform small, scoped, evidence-backed documentation or hygiene corrections
- Keeps branch policy and release gates aligned

## 4. Phase 2 Roadmap From Insight 0506

### P0 Reliability: Safety and Preflight Diagnosis

Goal:

Make the environment, workspace, permissions, providers, and tool risk visible before an agent starts real work.

Target outputs:

- `claude4net doctor`
- `claude4net doctor --output-format json`
- `PermissionMode` enum
- `PermissionEnforcer`
- `CommandRiskClassifier`
- CLI `--permission-mode` flag
- workspace-outside write block tests
- symlink escape tests
- binary write safety checks

Priority reason:

Developers cannot trust AI file and terminal automation until safety gates are predictable and inspectable.

### P1 Visibility: Observable and Resumable Sessions

Goal:

Make long-running agent work observable, resumable, and easy to audit.

Target outputs:

- `.claude4net/sessions/` standard layout
- `AgentSessionStore`
- `AgentTaskBoard`
- `AgentProgressWriter`
- `WorkflowGateEvaluator`
- `task-board.json`
- `progress-{agent}.jsonl`
- `result-{agent}.md`
- `claude4net status`
- `claude4net resume {sessionId}`
- Spectre.Console TUI task board

Priority reason:

Claude4Net-App must become an operations console for agent work, not only a chat loop.

### P2 Developer Workflow: Diff Approval and Execution Control

Goal:

Let users inspect and approve AI-proposed changes and risky terminal actions before they happen.

Target outputs:

- Diff viewer
- file-level approve/reject
- hunk-level approve/reject if feasible
- `IUserApprovalHandler` request model refinement
- dangerous command approval flow
- terminal execution status panel
- JSON status output standard

Priority reason:

Developers need to know what AI changed before trusting it with real repositories.

### P3 Intelligence: Skill Evolution and Reuse

Goal:

Make Claude4Net-App smarter over time by turning successful work patterns into tracked, versioned skill assets.

Target outputs:

- `SkillRegistryService`
- `.skill_id` sidecar support
- `SkillQualityTracker`
- `SkillEvolutionPlanner`
- `SkillLineageStore`
- `SkillIdResolver`
- skill quality metrics
- hallucinated skill recovery
- diff-based skill evolution proposal
- `!reflect` or background reflection job

Priority reason:

Reusable skills reduce repeated token cost and preserve project-specific agent knowledge.

## 5. Current Milestone

### K021 Gemini thought-signature/tool-turn compatibility hardening

Status: Completed

K018 P3 Skill Registry Foundation is completed and recorded in `IMPLEMENTATION_PROGRESS.md`.

Linux Cross-Platform Preparation is no longer part of the `experiment` branch plan. Linux work belongs on the dedicated Linux branch and must not be mixed into the experiment branch backlog.

Recently completed milestones:

- K015 P0 Reliability Preflight and Permission Foundation: Completed
- K016 P1 Session Store and Task Board: Completed
- K017 P2 Diff Approval Workflow: Completed
- K018 P3 Skill Registry Foundation: Completed
- K021 Gemini thought-signature/tool-turn compatibility hardening: Completed

Current decision point:

- choose the next `experiment` milestone from K022 or another explicitly approved item
- handle K020 only on `macoMaster`
- handle Linux preparation only on the dedicated Linux branch plan

## 6. Execution Rules

Before any milestone can be marked Completed, all conditions must be true:

1. implementation or document artifacts exist
2. required new files are not left untracked
3. staged content matches the actual scope
4. `.\scripts\verify-release.ps1` passes on Windows
5. `IMPLEMENTATION_PROGRESS.md` reflects the real result
6. this file points to the correct current and next milestone
7. no unresolved P1 issue remains
8. remaining risks are documented honestly

If any condition is missing, use one of:

- Partial
- In Progress
- Prepared
- Implemented, Not Operationalized
- Native Verification Pending

Do not use Completed unless the release gate and documentation both prove it.

## 7. Worker Prompt

```markdown
No worker prompt is active for the experiment branch.

Before assigning new work, Codex must explicitly choose one of:

1. K022 Skill evolution proposal workflow on `experiment`
2. K020 macOS Native Verification on `macoMaster`

Do not start Linux preparation from this plan. Linux preparation belongs to the dedicated Linux branch plan.

Universal rules for the next worker prompt:
- Do not modify `.agents/`.
- Do not claim build/test success before actually running commands.
- Do not commit or push.
- Keep staged scope narrow and milestone-specific.
- Update `IMPLEMENTATION_PROGRESS.md` only with verified facts.
- Update this plan only after the milestone result is known.

Verification:
1. `dotnet build -p:UseAppHost=false`
2. `dotnet build -p:UseAppHost=false -warnaserror:CS8600,CS8601,CS8602,CS8603,CS8604,CS8618,CS8620,CS8625`
3. `dotnet test .\Claude4Net.Tests\Claude4Net.Tests.csproj -p:UseAppHost=false`
4. `.\scripts\verify-release.ps1`

Final report:
1. overall status
2. completed scope
3. changed files
4. new file classification
5. core implementation summary
6. safety behavior summary
7. build result
8. strict build result
9. test result
10. release gate result
11. remaining risks
12. next execution prompt
13. commit/push status: not performed
```

## 8. Next Milestone Candidates

After K018, choose based on risk and first reviewer findings:

1. K022 Skill evolution proposal workflow on `experiment`
2. K020 macOS Native Verification on `macoMaster`

Do not start multiple milestone tracks in the same branch at the same time unless explicitly approved.
