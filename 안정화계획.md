# 안정화 계획

## 목표

대규모 기능 추가를 일시 중단하고, 현재 워킹트리에 섞여 있는 D05 이후 변경을 통합 가능한 안정 상태로 정리한다.

핵심 원칙:

- 신규 기능 추가 금지
- `git add`, `git commit`, `git push`는 Codex 검증 및 사용자 승인 전 금지
- `.agents/` 디렉터리 수정 금지
- `Documents/구현계획.md` 원본 훼손 금지
- 임시 로그 파일과 필수 소스 파일을 명확히 분리
- 빌드/테스트/CLI 실행을 실제 로그 기준으로만 성공 판정

## 100 Steps

001. 모든 Gemini/에이전트에게 신규 기능 추가 중단 지시
002. `git add`, `git commit`, `git push` 금지 재확인
003. 현재 브랜치가 `experiment`인지 확인
004. `git status --short --branch` 기록
005. `git log --oneline -10` 기록
006. 현재 untracked 파일 전체 목록 기록
007. 임시 로그 파일 목록 분리
008. 신규 소스 파일 목록 분리
009. 신규 리소스 파일 목록 분리
010. 변경된 tracked 파일 목록 분리

011. D05 변경 파일 그룹화
012. D06 변경 파일 그룹화
013. D07 변경 파일 그룹화
014. D08 변경 파일 그룹화
015. D09 변경 파일 그룹화
016. D11/RAG 변경 파일 그룹화
017. TeruTeruPandas 변경 파일 그룹화
018. 임시 산출물 그룹화
019. `IMPLEMENTATION_PROGRESS.md` 변경 내용 검토
020. 범위 밖 변경 여부 표시

021. untracked 신규 소스 중 필수 파일 식별
022. `GeminiEmbeddingProvider.cs` 필수 여부 확인
023. `CoordinatorStore.cs` 필수 여부 확인
024. `SelfHealingService.cs` 필수 여부 확인
025. `DiscordResponseFormatter.cs` 필수 여부 확인
026. `SelfHealingModels.cs` 필수 여부 확인
027. `VectorColumn.cs` 필수 여부 확인
028. `DataFrameVectorExtensions.cs` 필수 여부 확인
029. 신규 테스트 파일 필수 여부 확인
030. 임시 로그 파일은 커밋 제외로 확정

031. P1: untracked 필수 소스 누락 위험 해결 방침 확정
032. P1: Gemini tool result 매핑 설계 확정
033. P2: `.resources` 출력 복사 정책 확정
034. P2: RAG empty vector fallback 정책 확정
035. P2: Discord job 완료 처리 정책 확정
036. P3: D11 RAG 테스트 실효성 보강 방침 확정
037. AppState 전역 상태 변경 영향 검토
038. 테스트 collection 적용 누락 검토
039. `SystemBaseDir` setter 부작용 검토
040. `SelfHealingService.Instance` 경로 캡처 문제 검토

041. GeminiProvider tool call id와 function name 분리
042. tool result payload에 원래 function name 보존
043. 동일 도구 다중 호출 테스트 추가
044. Gemini history 변환 로직 테스트 추가
045. ClaudeService tool_use/tool_result 순서 테스트 추가
046. ClaudeService assistant content 보존 확인
047. provider별 history format 차이 문서화
048. provider 공통 tool result 모델 필요성 검토
049. provider 회귀 테스트 실행
050. provider 안정화 완료 표시

051. `.resources`를 출력 폴더로 복사할 위치 결정
052. `Claude4Net.Cli.csproj`에 리소스 복사 규칙 추가 여부 결정
053. repo root `.resources`와 `SystemBaseDir` 관계 정리
054. 테스트 전용 `SystemBaseDir` override 유지 여부 결정
055. 실제 CLI 실행에서 리소스 로드 확인
056. D06ResourceTests가 배포 경로도 검증하게 보강
057. `.resources/PandasDbTool` 커밋 포함 여부 결정
058. `.resources/weather_search` 커밋 포함 여부 확인
059. 누락 리소스 graceful fallback 확인
060. D06 안정화 완료 표시

061. RAG 저장 시 embedding 실패 정책 결정
062. 빈 vector 저장 금지 또는 null 처리 결정
063. semantic search 전 vector 차원 검증 추가
064. 차원 불일치 시 keyword fallback 적용
065. RAG 저장 실패 로그 유지
066. fire-and-forget 유지 여부 재검토
067. 필요 시 RAG 저장을 await 가능한 서비스로 분리
068. D11RAGTests에 실제 row 삽입 추가
069. semantic top match assertion 추가
070. RAG 안정화 완료 표시

071. DiscordOutputHandler에서 `WriteAsync`와 완료 처리 분리
072. job 시작 상태 전이만 `WriteAsync`에서 처리
073. 완료는 별도 complete/finalize 경로로 이동
074. 실패는 `NotifyFailureAsync`에서만 처리
075. 긴 메시지 분할 시 중복 완료 메시지 방지
076. Discord job duration 계산 검증
077. Discord formatter 테스트 보강
078. 실제 Discord token 없는 환경 fallback 확인
079. DiscordTests 재실행
080. D07 안정화 완료 표시

081. CoordinatorStore 중복 gate 방지 확인
082. D08PhaseGatesTests에 `[Collection("AppState")]` 적용 검토
083. D11RAGTests에 `[Collection("AppState")]` 적용 검토
084. AppState.Tasks 테스트 간 clear 정책 정리
085. PandasUniverseManager 테스트 격리 정책 정리
086. 테스트 병렬 실행 리스크 확인
087. 전체 테스트 2회 연속 실행
088. 일반 빌드 실행
089. strict nullable 빌드 실행
090. CLI `/exit` 파이프 실행

091. `git status --short --branch` 재확인
092. 커밋 포함 파일 후보 목록 작성
093. 커밋 제외 파일 후보 목록 작성
094. 임시 로그 파일 삭제 또는 ignore 후보로 분리
095. 범위 밖 D11/TeruTeruPandas 포함 여부 최종 결정
096. Codex 리뷰 findings 재실행
097. Gemini 수정 보고와 실제 diff 대조
098. 최종 안정화 보고 작성
099. 사용자 승인 후에만 stage
100. 사용자 승인 후 한글 커밋 메시지로 통합 커밋/푸시

## 완료 판정 기준

- 일반 빌드 성공
- strict nullable 빌드 성공
- 전체 테스트 2회 연속 성공
- CLI `/exit` 파이프 실행 성공
- 신규 필수 소스 파일 누락 없음
- 임시 산출물 커밋 제외
- Codex 리뷰 P1/P2 finding 해소
- 사용자 승인 전 커밋/푸시 없음
