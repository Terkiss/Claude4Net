# 🔥 Claude4Net을 위한 OpenSpace 리버스 엔지니어링 딥다이브 인사이트

## 1. 거시적 아키텍처 철학 (Macro Architecture)

OpenSpace의 핵심 철학은 **"자율적 진화(Autonomous Evolution)"** 와 **"실행 궤적 기반 피드백(Trajectory-based Feedback)"** 입니다.

### 1) 시스템 뼈대 및 데이터 흐름
- **Agent Loop (Grounding Agent):** LLM이 작업을 분석하고 툴을 선택 -> 툴 실행 -> `traj.jsonl`에 실행 궤적 저장 -> 컨텍스트 업데이트 -> LLM 재호출의 뫼비우스 루프를 돕니다. 명시적인 `<COMPLETE>` 토큰을 반환할 때까지 반복됩니다.
- **Skill Engine (스킬 진화 시스템):** SQLite DB(`SkillStore`)를 활용하여 각 툴/스킬의 메트릭(`total_selections`, `total_applied`, `total_completions`)을 원자적으로 기록합니다. 
- **Analyzer & Evolver:** `ExecutionAnalyzer`가 `traj.jsonl`을 분석하여 실패 원인(Semantics)을 LLM에 제공하고, `SkillEvolver`가 SKILL.md 파일을 패치하여 기술을 진화(`FIX`, `DERIVED`, `CAPTURED`)시킵니다.
- **Tool Layer & MCP Server:** `mcp_server.py`를 통해 FastMCP를 초기화하며, `execute_task`, `search_skills`, `fix_skill` 등의 함수를 외부(로컬 호스트 또는 클라우드)로 노출합니다.

### 2) 숨겨진 디테일 및 마이크로 패턴
- **MCP Stdio 보호 트릭 (`_MCPSafeStdout`):** 
  가장 중요한 엔지니어링 디테일 중 하나입니다. Stdio를 통한 MCP 통신 시, Agent의 무거운 로깅(stdout)이 파이프 버퍼를 채워 데드락(Deadlock)을 유발하는 현상을 막기 위해, 바이너리 출력(`.buffer`)은 실제 `stdout`으로 보내고 텍스트 출력(`.write`)은 `stderr`로 리다이렉트하는 래퍼 클래스를 사용했습니다.
- **유연한 응답 파싱 (Fuzzy Fallback):**
  JSON 응답이 깨질 경우에 대비해 정규식을 활용한 휴리스틱 파싱(`\byes\b`, `\bconfirm\w*\b`)으로 불완전한 LLM 출력을 방어합니다.
- **퍼지 매칭을 위한 공백 정규화:**
  코드 검색/치환(`seek_sequence`, `whitespace_normalized_replacer`) 시 `\s+`를 단일 공백으로 치환하여 들여쓰기 변형으로 인한 패치 실패를 방어합니다.
- **안전성 검증 필터 (Safety Rules):**
  정규식을 통해 `blocked.malware`, `suspicious.crypto`, `suspicious.secrets` 등을 필터링하여 에이전트가 위험한 스크립트나 개인 키를 추출하는 것을 원천 차단합니다.

---

## 2. Claude4Net을 위한 '초정밀' 액션 아이템 (C# 최적화)

파이썬의 동적 특성에 의존하던 로직을 C#의 강력한 타입 시스템과 최신 문법(Channels, Source Generators, Regex Generator 등)으로 치환하여 극한의 성능과 안정성을 확보합니다.

### [액션 1] MCP StdOut 데드락 방지 래퍼 (McpSafeTextWriter)
Python의 `_MCPSafeStdout` 패턴을 C#으로 완벽히 이식합니다. MCP 프로토콜의 JSON-RPC는 바이너리/기본 스트림으로 내보내고, 일반 `Console.WriteLine`은 `Console.Error`로 우회시킵니다.

```csharp
public class McpSafeTextWriter : TextWriter
{
    private readonly TextWriter _stderr;
    private readonly Stream _baseStdoutStream;

    public McpSafeTextWriter(Stream baseStdoutStream, TextWriter stderr)
    {
        _baseStdoutStream = baseStdoutStream;
        _stderr = stderr;
    }

    public override Encoding Encoding => _stderr.Encoding;

    // 일반 텍스트 출력은 무조건 Error 스트림으로 보내어 버퍼 점유율 데드락 방지
    public override void Write(char value) => _stderr.Write(value);
    public override void Write(string? value) => _stderr.Write(value);
    
    // MCP 메시지를 쏠 때만 Base Stream을 직접 사용 (BinaryWriter 활용 권장)
    public Stream GetMcpTransportStream() => _baseStdoutStream;
}

// Program.cs
Console.SetOut(new McpSafeTextWriter(Console.OpenStandardOutput(), Console.Error));
```

### [액션 2] 고성능 텍스트 파서 및 패치 엔진 (Regex Generator 적용)
런타임에 정규식을 컴파일하는 파이썬과 달리, C# 11+의 `[GeneratedRegex]`를 활용해 컴파일 타임에 최적화된 오토마타를 생성합니다.

```csharp
using System.Text.RegularExpressions;

public partial class PatternEngine
{
    // YAML Frontmatter 파서
    [GeneratedRegex(@"^---\r?\n(.*?)\r?\n---", RegexOptions.Singleline | RegexOptions.Compiled)]
    public static partial Regex YamlFrontmatter();

    // Search & Replace 패치 포맷 파서 (<<<<<<< SEARCH ... ======= ... >>>>>>> REPLACE)
    [GeneratedRegex(@"<{7}\s*SEARCH\s*\r?\n(.*?)\r?\n\s*={7}\s*\r?\n(.*?)\r?\n\s*>{7}\s*REPLACE\s*", RegexOptions.Singleline | RegexOptions.Compiled)]
    public static partial Regex DiffPatchPattern();

    // 마크다운 JSON 코드블록 클리너
    [GeneratedRegex(@"^```(?:json)?\s*\r?\n?(.*?)\r?\n?```", RegexOptions.Singleline | RegexOptions.Compiled)]
    public static partial Regex JsonCodeBlock();
}
```

### [액션 3] System.Threading.Channels 기반 Event-Driven Agent Loop
Python의 `asyncio` 루프를 C#의 `Channel<T>` 기반 Actor 모델로 변경하여 상태 관리와 툴 실행 병렬성을 극대화합니다.

```csharp
using System.Threading.Channels;

public record AgentEvent;
public record ToolExecutionRequest(string ToolName, string Arguments) : AgentEvent;
public record ToolExecutionResult(string ToolName, string Output, bool IsError) : AgentEvent;

public class GroundingAgentLoop
{
    private readonly Channel<AgentEvent> _eventBus = Channel.CreateUnbounded<AgentEvent>();

    public async Task RunAsync(CancellationToken ct)
    {
        await foreach (var evt in _eventBus.Reader.ReadAllAsync(ct))
        {
            switch (evt)
            {
                case ToolExecutionRequest req:
                    // 툴 실행 (비동기 Task Fire & Forget 또는 Wait)
                    _ = Task.Run(async () => {
                        var result = await ToolLayer.ExecuteAsync(req);
                        await _eventBus.Writer.WriteAsync(new ToolExecutionResult(req.ToolName, result, false), ct);
                    }, ct);
                    break;
                    
                case ToolExecutionResult res:
                    // 컨텍스트 업데이트 후 LLM 재호출
                    await ProcessLlmNextIterationAsync(res, ct);
                    break;
            }
        }
    }
}
```

### [액션 4] Source Generators를 통한 무손실 프롬프트 렌더링
`Prompts` 클래스의 `format` 호출 비용을 없애기 위해, 인터폴레이션 문자열을 컴파일 타임 코드로 생성하는 패턴을 제안합니다. 이는 LLM 호출 시마다 발생하는 GC 오버헤드를 줄입니다.

```csharp
// ISourceGenerator를 활용하여 인터페이스를 구현하는 팩토리 생성 방식을 사용하거나,
// 간단하게 정적 메서드와 C# 10+의 보간 문자열 핸들러(Interpolated String Handlers)를 사용합니다.
public static class GroundingAgentPrompts
{
    public const string TaskCompleteToken = "<COMPLETE>";

    public static string BuildSystemPrompt(bool hasMcp, bool hasShell)
    {
        var builder = new DefaultInterpolatedStringHandler(100, 2);
        builder.AppendLiteral("You are a Grounding Agent. Execute tasks using tools.\n");
        if (hasMcp) builder.AppendLiteral("- **MCP tools** are typically faster.\n");
        if (hasShell) builder.AppendLiteral("- **Shell tools** are fast and accurate.\n");
        builder.AppendLiteral($"\nTask Completion Token: {TaskCompleteToken}");
        return builder.ToStringAndClear();
    }
}
```

### [액션 5] IAsyncEnumerable 기반 SQLite 분석 및 메트릭 업데이트 (SkillStore)
잦은 DB 업데이트로 인한 Lock 이슈(WAL 모드로 회피한 약점)를 C#에서는 Dapper와 `IAsyncEnumerable` 기반 비동기 스트림으로 지연 처리 및 일괄 배치 업데이트로 승화시킵니다.

```csharp
public class SkillStore
{
    private readonly Channel<SkillMetricUpdate> _metricQueue = Channel.CreateUnbounded<SkillMetricUpdate>();

    // 생성자에서 백그라운드 Worker 실행
    public SkillStore()
    {
        _ = ProcessMetricsBatchAsync();
    }

    public void RecordSelection(string skillId) => 
        _metricQueue.Writer.TryWrite(new SkillMetricUpdate(skillId, MetricType.Selection));

    private async Task ProcessMetricsBatchAsync()
    {
        // 큐에서 아이템을 모아 벌크 UPDATE 쿼리 수행 (SQLite 쓰기 락 최소화)
        var buffer = new List<SkillMetricUpdate>();
        await foreach (var update in _metricQueue.Reader.ReadAllAsync())
        {
            buffer.Add(update);
            if (buffer.Count >= 50 || _metricQueue.Reader.Count == 0)
            {
                await FlushToDatabaseAsync(buffer);
                buffer.Clear();
            }
        }
    }
}
```

---

## 3. Claude4Net 특화: 자율적 진화 & TeruTeruPandas 통합 전략

Claude4Net의 독보적인 강점인 **TeruTeruPandas(고성능 C# 데이터프레임)**를 활용하여, 실행 로그(Trajectory)를 단순한 텍스트 분석이 아닌 **'데이터 사이언스'** 관점에서 접근합니다.

### 1) 실행 궤적의 데이터프레임화 (Log Ingestion)
- 에이전트의 모든 활동(`traj.jsonl`)을 `TeruTeruPandas`로 즉시 로드합니다.
- 단순한 문자열 검색이 아니라, 통계적으로 유의미한 실패 패턴을 감지합니다.

### 2) LLM 전처리를 위한 통계적 요약 (Intelligence Extraction)
- 수천 줄의 로그 전체를 LLM에 전달하는 대신, `TeruTeruPandas`가 정제한 **'진단서'**를 제공하여 토큰 비용을 90% 절감하고 분석 정확도를 높입니다.

```csharp
// TeruTeruPandas를 활용한 로그 분석 예시
public class TeruTeruEvolutionAnalyzer
{
    public async Task<string> GenerateDiagnosticReport(string trajPath)
    {
        // JSONL 로그를 데이터프레임으로 로드
        var df = Pandas.ReadJsonL(trajPath);

        // 실패(error/incomplete)한 툴 실행 정보만 필터링
        var failDf = df.Where(r => r["status"].ToString().Contains("fail"));
        
        // 툴별 실패 빈도 계산
        var toolStats = failDf.GroupBy("tool_name").Count();

        // LLM에게 전달할 고밀도 인사이트 생성
        var sb = new StringBuilder();
        sb.AppendLine("## [TeruTeruPandas] 실행 패턴 분석 결과");
        foreach(var row in toolStats.Rows)
        {
            sb.AppendLine($"- 툴 '{row["tool_name"]}': {row["count"]}회 실패 감지됨");
        }
        return sb.ToString();
    }
}
```

### 3) '데이터 기반' 스킬 강화 루프
- **Step 1 (기억):** `TrajectoryStore`에 모든 실행 내역을 축적.
- **Step 2 (분석):** `TeruTeruPandas`로 '가장 자주 실패하는 지점'과 '성공적인 툴 조합'을 통계적으로 추출.
- **Step 3 (진화):** 추출된 인사이트를 바탕으로 LLM이 `SKILL.md`를 수정.
- **Step 4 (검증):** 수정된 스킬이 적용된 가상 시뮬레이션을 수행하여 성공률이 개선되는지 데이터로 확인.

이러한 통합 전략을 통해 Claude4Net은 단순한 챗봇 OS가 아닌, **데이터 기반으로 스스로를 최적화하는 '자율 학습형 AI 커널'**로 진화할 수 있습니다.
