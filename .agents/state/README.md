# Runtime State

This directory stores Terukirdo runtime state files (execution ledger, task records).

All files here are auto-generated and excluded from version control via `.gitignore`.

## Contents

- `execution_ledger.json` — Active execution cards and task states (auto-generated)
- `evidence_bundles/` — Per-task evidence bundles (auto-generated)

`.gitkeep` is tracked to preserve this directory in Git while excluding generated files.
